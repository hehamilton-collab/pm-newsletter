# PM Newsletter Configuration
# Used by both the Claude Code slash command and the standalone Python script

user:
  name: "Heaven Hamilton"
  slack_user_id: "U052DK3HMRC"
  email: "hehamilton@ebay.com"
  timezone: "America/Los_Angeles"

jira:
  project_key: "ATMOS"
  initiative: "DP Gateway"
  components:
    - "Recoplex"
    - "Caramel"
    - "Config Hub"
    - "DP Gateway"
  priority_watch:
    - "P1"
    - "P2"

slack:
  decision_keywords:
    - "decision"
    - "agreed"
    - "approved"
    - "shipping"
    - "aligned"
    - "sign off"
    - "go/no-go"
  lookback_days: 7

newsletter:
  output_dir: "~/pm-newsletter/output"
  format: "markdown"  # Will switch to "google_doc" in Phase 2

# Phase 2 - uncomment when credentials are ready
# google:
#   credentials_file: "~/.config/pm-newsletter/google_credentials.json"
#   token_file: "~/.config/pm-newsletter/google_token.json"
#   newsletter_folder_id: ""  # Google Drive folder ID for newsletter output
#
# outlook:
#   client_id: ""
#   tenant_id: ""
#   token_file: "~/.config/pm-newsletter/outlook_token.json"
